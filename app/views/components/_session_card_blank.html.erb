<div class="rounded-lg p-3 flex flex-col gap-y-2 h-full border-2 border-dashed border-shade-30">
    <p class="text-sub">
        Schedule.blank?
    </p>
    <div class="flex flex-col gap-y-1 text-sub font-bold">
        <%= t('card.friends') %>
        <% 
            dummy_profiles = [
                Data.define(:name, :avatar_url, :id).new("kinoppyd", "https://i.pravatar.cc/150?img=1", 1),
                Data.define(:name, :avatar_url, :id).new("kinoppyd", "https://i.pravatar.cc/150?img=2", 1),
                Data.define(:name, :avatar_url, :id).new("kinoppyd", "https://i.pravatar.cc/150?img=3", 1),
                Data.define(:name, :avatar_url, :id).new("kinoppyd", "https://i.pravatar.cc/150?img=4", 1),
                Data.define(:name, :avatar_url, :id).new("kinoppyd", "https://i.pravatar.cc/150?img=5", 1),
            ]
        %>
        <%= render 'components/friends_button', profiles: dummy_profiles, onClick: "alert('test')" %>
    </div>
    <% if @team.present? %>
        <div class="flex flex-col gap-y-1 text-sub font-bold">
            <%= t('card.team', team_name: @team&.name) %>
        </div> 
        <%# FIXME: チームメンバーのプロフィールを表示 %>
    <% end %>
</div> 